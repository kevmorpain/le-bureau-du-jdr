# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

.wrangler/
# Secrets locaux de `wrangler dev` (cf. CLAUDE.md — on préfère `--var`, mais ne jamais
# committer ce fichier s'il est créé).
.dev.vars
.dev.vars.*

# Claude
.claude/settings.local.json

# Local env files
.env
.env.*
!.env.example

.claude/worktrees/